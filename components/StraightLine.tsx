import React from 'react';
import { View } from 'react-native';

const StraightLine = () => {
  return (
    <View
      style={{
        height: 1, // adjust the height of the line as needed
        backgroundColor: 'black', // adjust the color of the line as needed
      }}
    />
  );
};

export default StraightLine;
